//YES / NO
//ON / OFF
//TRUE / FALSE

var isLoggedIn = true;
var isAuthenticated = true;
var hasLoggedInToday = false;
var hasToken = false;

console.log(2 > 1); //true
console.log(3 < 2); //false

var test = 2 >= 3; //What will this print?
console.log(test);
console.log("Two is greater than one? " + (2 > 1));
console.log(3 >= 1);

console.log("&& :", 2===2 && 1===1 ); //true because?
console.log("|| :", 2===2 || 2===1 ); //true because?
console.log("!=", 2 != 1) //true because?

//rule about == versus ===. == checks to see if the values are the same. Not the type. === checks to see if the values and the equality are the same.
// || means 'or'
// && means 'and'
// and ! means 'not' or opposite, like == is equal to, != is not equal to 


